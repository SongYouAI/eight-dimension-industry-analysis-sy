# 行业分析数据源配置

> 最后更新：2026-06-04

## 数据源清单

| # | Skill 名称 | 定位 | 数据类型 | 搜索优先级 |
|---|-----------|------|----------|-----------|
| 1 | `westock-data` | 结构化金融数据 | 行业分类、成份股、财报、研报、一致预期 | ★★★ 核心 |
| 2 | `neodata-financial-search` | 自然语言金融搜索 | 行业总量、估值、景气度、新闻公告 | ★★ 补充 |
| 3 | `WebSearch` + `WebFetch` | 公开信息检索 | 行业报告、政策文件、产业链 | ★★ 定性 |
| 4 | `wechat-article-search` | 微信深度文章 | 行业深度研究、专家观点 | ★ 点缀 |
| 5 | `competitive-analysis` | 竞争分析框架 | 波特五力、SWOT、对标分析 | 按需加载 |

## 调用策略

### 搜索顺序

1. **先调用 `westock-data`**（结构化数据）：获取行业板块分类、龙头公司、财务数据
   - ⚠️ **环境路由（运行时注）**：若当前环境 `westock-data` **CLI 不可用**，改用 `mcp__westock-mcp`（腾讯自选股 MCP，工具名以 `data_` 前缀）；`neodata-financial-search` 走其 `scripts/query.py` + 凭证（12h 时效，tempToken 优先）。具体调用见对应 skill。运行时细节不写进 SKILL.md 正文，避免与环境耦合。
2. **再调用 `neodata-financial-search`**（语义搜索）：补充行业级指标和动态
3. **最后 `WebSearch` + `wechat-article-search`**（定性信息）：政策、趋势、深度观点

### 调用效率

- Phase 1 数据采集分 4 组并行（A/B/C/D），每组 2-3 个调用
- 预计 4-5 轮可完成全部数据采集
- 高权重维度优先采集完整数据，低权重维度用简短摘要

### Token 节约

- `westock-data` 财报调用取核心指标（营收、利润、ROE、ROIC），不拉全量报表
- `WebSearch` 取前 3-5 条搜索结果摘要
- `neodata-financial-search` 取结构化返回的关键数据
- 所有搜索结果只取核心要点，不全量读取

## westock-data 调用示例

```
# 查行业板块分类
"帮我查一下 申万行业分类 中 '新能源汽车' 对应的行业板块"

# 查板块行情
"帮我查一下 新能源车 板块的指数走势"

# 查龙头公司
"帮我查一下 新能源车板块的前10大成份股"

# 查个股财报
"帮我查一下 宁德时代 的财务报表，只看营收、净利润、ROE"

# 查研报
"帮我查一下 宁德时代 的机构研报"
```

## neodata-financial-search 调用示例

```
# 行业总量搜索
"新能源车行业 市场规模 2024 2025 增长率"

# 估值搜索
"新能源车行业 平均PE PB 估值水平"

# 景气度搜索
"新能源车 行业景气度 景气指数 产销数据"
```

## WebSearch 调用示例

```
# 产业链搜索
"新能源汽车 产业链 上下游 图谱 2025"

# 政策搜索
"新能源汽车 产业政策 补贴 法规 2025 2026"

# 市场规模搜索
"新能源汽车 全球市场规模 TAM 展望 2025-2030"
```
